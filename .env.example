# Database (PostgreSQL)
# Example: postgresql://user:password@localhost:5432/dbname
# For local development: postgresql://postgres:postgres@localhost:5432/lhr_hessen
# For cloud providers like Neon, Supabase, or Railway, use their connection string
DATABASE_URL=postgresql://user:password@localhost:5432/lhr_hessen

# JWT Secret for one-time tokens
JWT_SECRET=your-very-secure-random-secret-key

# Resend API Key for sending emails
RESEND_API_KEY=your-resend-api-key

# Application URL (for generating voting links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
